#pragma once

#include <glad/glad.h>
#include <SDL/SDL.h>

#include <string>
#include <sstream>
#include <fstream>
#include <iostream>

class Shader {
 public:
  std::string readFile(const char* file);
  void bindAttributeLocation(int index, const std::string& attribute);
  void useProgram();
  bool init();
};
